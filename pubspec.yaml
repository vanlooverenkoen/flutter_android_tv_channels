name: android_tv_channels
description: A new Flutter project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: be.vanlooverenkoen.androidtvchannels.flutter_android_tv_channels
        pluginClass: AndroidTvChannelsPlugin